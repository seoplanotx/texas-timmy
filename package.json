{
  "name": "texas-timmy",
  "version": "1.0.0",
  "private": true,
  "description": "NES-style shmup with instrumented viral loops — the game itself is a single dependency-free index.html; these deps power the optional Vercel edge backend.",
  "dependencies": {
    "@vercel/edge": "^1.2.2",
    "@vercel/og": "^0.8.5",
    "react": "^19.0.0"
  }
}
