{
  "name": "texas-timmy",
  "version": "1.0.0",
  "private": true,
  "description": "NES-style shmup with instrumented viral loops — the game itself is a single dependency-free index.html; this dep powers the optional Vercel edge middleware.",
  "dependencies": {
    "@vercel/edge": "^1.2.2"
  }
}
